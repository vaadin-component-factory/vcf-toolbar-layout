import { registerStyles, css } from '@vaadin/vaadin-themable-mixin/vaadin-themable-mixin.js';

// todo: move styles here
registerStyles(
  'vcf-toolbar-layout',
  css`
  
    :host {
      /* Variables for the toolbar layout */
    }
  `
);
